import {describe,it,expect} from 'vitest';import{filterAuthorizedMenuItems,hasDiaAuthority,type DiaMenuItem,type DiaUserAuthorizations}from'./index';
const user:DiaUserAuthorizations={authorities:['admin'],roles:['seller'],permissions:['orders.read','orders.write']};
describe('hasDiaAuthority',()=>{it('allows empty requirements',()=>{expect(hasDiaAuthority(undefined)).toBe(true);expect(hasDiaAuthority(user,{})).toBe(true)});it('matches any by default',()=>{expect(hasDiaAuthority(user,{roles:['buyer'],permissions:['orders.read']})).toBe(true);expect(hasDiaAuthority(user,{roles:['buyer'],permissions:['none']})).toBe(false)});it('requires all declared groups for all mode',()=>{expect(hasDiaAuthority(user,{match:'all',roles:['seller'],permissions:['orders.read','orders.write']})).toBe(true);expect(hasDiaAuthority(user,{match:'all',roles:['seller'],permissions:['missing']})).toBe(false)})});
describe('filterAuthorizedMenuItems',()=>{it('removes hidden unauthorized and empty parents recursively',()=>{const items:DiaMenuItem[]=[{id:'root',label:'Root',children:[{id:'ok',label:'Orders',href:'/orders',requirement:{permissions:['orders.read']}},{id:'no',label:'No',href:'/no',requirement:{roles:['admin']}},{id:'hidden',label:'Hidden',href:'/h',hidden:true}]},{id:'empty',label:'Empty',children:[{id:'x',label:'X',href:'/x',requirement:{permissions:['x']}}]}];expect(filterAuthorizedMenuItems(items,user)).toEqual([{id:'root',label:'Root',children:[{id:'ok',label:'Orders',href:'/orders',requirement:{permissions:['orders.read']}}]}])})});
