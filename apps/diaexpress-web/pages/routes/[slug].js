import Link from 'next/link';
import SeoHead from '../../src/components/seo/SeoHead';
import { routePages } from '../../src/content/public/marketingPages';
export default function RouteLanding({ page }){return <main className="dx-public-page"><SeoHead title={`${page.origin} vers ${page.destination} | DiaExpress`} description={`Expédier de ${page.origin} vers ${page.destination}: délais, options et devis DiaExpress.`} path={`/routes/${page.slug}`} /><section className="dx-hero"><p className="dx-eyebrow">Route populaire</p><h1>{page.origin} → {page.destination}</h1><p>Corridor SEO pilotable par le CMS routes populaires pour préparer l&apos;expansion régionale.</p><div className="dx-actions"><Link href="/quote-request" className="dx-btn dx-btn-primary">Demander un devis</Link><Link href="/track-shipment" className="dx-btn dx-btn-secondary">Suivre un colis</Link></div></section><section className="dx-grid"><article className="dx-card"><h2>Transport</h2><p>{page.transport}</p></article><article className="dx-card"><h2>Délai estimé</h2><p>{page.delay}</p></article><article className="dx-card"><h2>Pourquoi DiaExpress ?</h2><ul><li>Tracking rapide</li><li>Support régional</li><li>Prix confirmé par devis</li></ul></article></section></main>}
export function getStaticPaths(){return {paths:Object.keys(routePages).map((slug)=>({params:{slug}})),fallback:false};}
export function getStaticProps({params}){return {props:{page:{...routePages[params.slug],slug:params.slug}}};}
