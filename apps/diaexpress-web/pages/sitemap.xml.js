import { servicePages, routePages } from '../src/content/public/marketingPages';
function Sitemap(){return null}export async function getServerSideProps({res}){const site=process.env.NEXT_PUBLIC_SITE_URL||'https://www.diaexpress.com';const paths=['/','/services','/quote-request','/quote-lead','/track-shipment','/contact',...Object.keys(servicePages).map(s=>`/services/${s}`),...Object.keys(routePages).map(s=>`/routes/${s}`)];res.setHeader('Content-Type','text/xml');res.write(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${site}${p}</loc></url>`).join('')}</urlset>`);res.end();return {props:{}}}export default Sitemap;
