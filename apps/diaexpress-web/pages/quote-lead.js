import React from 'react';
import SeoHead from '../src/components/seo/SeoHead';
const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:5000';
export default function QuoteLead(){const [form,setForm]=React.useState({name:'',phone:'',email:'',country:'',need:''});const [status,setStatus]=React.useState('');const set=(k)=>(e)=>setForm({...form,[k]:e.target.value});async function submit(e){e.preventDefault();setStatus('Envoi...');try{await fetch(`${API_BASE_URL}/api/public/quote-leads`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setStatus('Merci, un conseiller DiaExpress vous recontacte rapidement.');setForm({name:'',phone:'',email:'',country:'',need:''});}catch{setStatus('Votre demande est préparée. Contactez le support si besoin.')}}return <main className="dx-public-page"><SeoHead title="Lead devis rapide | DiaExpress" description="Laissez vos coordonnées et votre besoin logistique sans compléter un devis complet." path="/quote-lead" /><section className="dx-hero"><p className="dx-eyebrow">Lead generation</p><h1>Parlez-nous rapidement de votre besoin</h1><p>Idéal si vous n’avez pas encore tous les détails pour créer un devis complet.</p></section><form className="dx-card" style={{maxWidth:760,margin:'0 auto'}} onSubmit={submit}>{['name','phone','email','country'].map((k)=><label key={k} style={{display:'block',marginBottom:12}}>{k}<input required={k==='name'||k==='phone'} value={form[k]} onChange={set(k)} style={{width:'100%',padding:12,borderRadius:12,border:'1px solid #cbd5e1'}} /></label>)}<label style={{display:'block',marginBottom:12}}>besoin<textarea required value={form.need} onChange={set('need')} style={{width:'100%',padding:12,borderRadius:12,border:'1px solid #cbd5e1'}} /></label><button className="dx-btn dx-btn-primary">Envoyer ma demande</button>{status && <p>{status}</p>}</form></main>}
