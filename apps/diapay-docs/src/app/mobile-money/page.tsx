export default function Page(){return <main className="portal"><section className="content"><h1>Diapay mobile money</h1><p>Clear developer guide with copy-ready curl, JavaScript fetch and Node SDK examples, success/error responses, common pitfalls and links to the sandbox.</p><pre>{`curl -X POST http://localhost:5100/api/v1/payments -H "Authorization: Bearer sk_test_***" -H "Content-Type: application/json" -d '{"amount":25000,"currency":"XOF"}'`}</pre><pre>{`const diapay = new Diapay({ secretKey: process.env.DIAPAY_SECRET_KEY });
// Use sandbox first, then live after approval.`}</pre><h2>Mobile Money Africa Testing</h2><p>Orange Money success/pending, Moov Money failed, Wave timeout, MTN OTP required and USSD flow are mock placeholders only. Fields: phoneNumber, country, operator, currency, otp, ussdCode, providerReference and async webhook.</p><p><a href="http://localhost:3102/sandbox">Open sandbox</a></p></section></main>}