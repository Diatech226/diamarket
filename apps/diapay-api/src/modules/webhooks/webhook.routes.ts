import { Router } from 'express'; import { receiveProviderWebhook } from './provider-webhook.controller'; import { createEndpoint, deleteEndpoint, getEndpoint, listDeliveries, listEndpoints, listEvents, patchEndpoint } from './merchant-webhook.controller';
export const webhookRouter=Router(); webhookRouter.post('/webhooks/providers/:provider', receiveProviderWebhook); webhookRouter.get('/webhook-endpoints', listEndpoints); webhookRouter.post('/webhook-endpoints', createEndpoint); webhookRouter.get('/webhook-endpoints/:id', getEndpoint); webhookRouter.patch('/webhook-endpoints/:id', patchEndpoint); webhookRouter.delete('/webhook-endpoints/:id', deleteEndpoint); webhookRouter.get('/webhook-events', listEvents); webhookRouter.get('/webhook-deliveries', listDeliveries); webhookRouter.get('/logs', listDeliveries);
