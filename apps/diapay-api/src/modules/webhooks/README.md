# Diapay module placeholder

Legacy route kept for compatibility during Diapay restructuring.

This directory is reserved for the progressive modular migration. Public `/api/v1` routes remain mounted through the legacy router until each domain is moved safely behind contract tests.
