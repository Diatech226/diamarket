export function sanitizePayload(value: unknown): unknown { if (Array.isArray(value)) return value.map(sanitizePayload); if (value && typeof value === 'object') { const out: Record<string, unknown> = {}; for (const [k,v] of Object.entries(value)) out[k] = /secret|token|key|otp|password|authorization|signature/i.test(k) ? '[redacted]' : sanitizePayload(v); return out; } return value; }
export function assertUrl(url: unknown): asserts url is string { if (typeof url !== 'string' || !/^https?:\/\//.test(url)) throw Object.assign(new Error('url must be an http(s) URL'), { status: 400, code: 'VALIDATION_ERROR' }); }
