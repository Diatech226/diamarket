export default function WebhookEndpointDetail({ params }: { params: { id: string } }) { return <main><h1>Webhook endpoint {params.id}</h1><p>Endpoint details include enabled events, latest delivery status codes, retry attempts, and a manual retry placeholder. The endpoint secret is only visible on creation and is masked here.</p><section><h2>Delivery health</h2><ul><li>Successful deliveries: shown from delivery logs</li><li>Failed deliveries: retryable 5xx/timeouts are queued</li><li>Last status code: sanitized from the latest attempt</li><li>Manual retry: prepared for persistent delivery storage</li></ul></section></main>; }
