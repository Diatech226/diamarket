import { Badge, Card, PageHeader } from '../../../components/ui';
const logs = ['checkout.session.created','payment.paid','webhook.delivered','api_key.rotated','sandbox.payment_failed'].map((message,i)=>({id:`log_${i}`,level:i===4?'warning':'info',message,createdAt:new Date(Date.now()-i*3600000).toISOString()}));
export default function LogsPage(){return <><PageHeader title="Logs" description="Audit trail merchant/fleet admin : API, webhooks, sandbox et sécurité sans exposer les secrets."/><Card>{logs.map(log=><div key={log.id} className="flex items-center justify-between border-b py-4 last:border-0"><div><p className="font-semibold">{log.message}</p><p className="text-xs text-slate-400">{log.id} · {new Date(log.createdAt).toLocaleString('fr-FR')}</p></div><Badge value={log.level === 'warning' ? 'pending' : 'delivered'}/></div>)}</Card></>}
