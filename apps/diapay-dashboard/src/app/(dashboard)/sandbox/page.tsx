import { Badge, Button, Card, PageHeader } from '../../../components/ui';
const scenarios = ['paiement réussi','paiement échoué','timeout mobile money','OTP expiré','solde insuffisant','provider indisponible','webhook répété','refund test'];
export default function SandboxPage(){return <><PageHeader title="Sandbox" description="Simulateur développeur : chaque scénario produit payment, event, webhook et log sans appeler de vrai provider."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{scenarios.map(s=><Card key={s}><div className="flex items-start justify-between gap-3"><h2 className="font-semibold capitalize">{s}</h2><Badge value="ready"/></div><p className="mt-2 text-sm text-slate-500">Génère un paiement test, un événement signé et une entrée de log.</p><Button variant="secondary">Simuler</Button></Card>)}</div></>}
