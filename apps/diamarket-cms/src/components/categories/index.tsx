import { resolveMediaUrl } from "@/lib/api";
import type { CategoryItem } from "@/types/cms";
export function CategoryTree({ categories }: { categories: CategoryItem[] }) { return <div className="rounded-2xl border border-stone-200 bg-white p-4"><h3 className="font-semibold">Arborescence catalogue</h3><ul className="mt-3 space-y-2">{categories.map((c) => <li key={c._id} className="flex items-center justify-between rounded-xl bg-stone-50 px-3 py-2"><span>{c.name}</span><span className="text-xs text-stone-500">ordre {c.order ?? 0}</span></li>)}</ul></div>; }
export function CategoryPreviewCard({ category }: { category?: Partial<CategoryItem> }) { return <aside className="rounded-2xl border border-amber-200 bg-amber-50 p-4"><p className="text-xs uppercase tracking-[0.25em] text-amber-800">Preview</p>{category?.image && <img src={resolveMediaUrl(category.image)} alt={category.name || "Catégorie"} className="mt-3 h-32 w-full rounded-xl object-cover"/>}<h3 className="mt-3 text-xl font-semibold">{category?.name || "Nouvelle catégorie"}</h3><p className="text-sm text-stone-600">{category?.description || "SEO, statut, parent/enfant et image seront prévisualisés ici."}</p></aside>; }
export function CategoryForm({ mode = "create" }: { mode?: "create" | "edit" }) { return <div className="rounded-2xl border border-stone-200 bg-white p-5"><h2 className="text-lg font-semibold">{mode === "edit" ? "Édition catégorie" : "Création catégorie"}</h2><p className="mt-1 text-sm text-stone-500">Sections identité, SEO, image, ordre, statut actif/inactif et parent/enfant.</p></div>; }
